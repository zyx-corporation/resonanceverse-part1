# --- Core Deep Learning Framework ---
torch>=2.1.0
torchvision
torchaudio

# --- NLP & SLM Integration ---
transformers>=4.38.0    # Llama-3, Gemma等の最新SLMサポート
datasets                # ベンチマークデータのロード用
accelerate>=0.27.0      # 推論速度とメモリの最適化
sentencepiece
protobuf

# --- Mathematical & Scientific Computing ---
numpy>=1.24.0           # 共鳴テンソル演算の補助
scipy                   # 統計的評価とKLダイバージェンス計算
scikit-learn            # 次元選択時の相互情報量スコアリング
matplotlib>=3.7.0       # Phase II-A 等の実験グラフ（tau 掃引）

# --- Configuration & Utilities ---
pyyaml                  # configs/ フォルダの設定ファイル読み込み用
tqdm                    # 学習進捗の可視化
einops                  # 多次元テンソル（共鳴場）の柔軟な変形用
streamlit>=1.33.0       # v7_pair_chat_app: st.container(height=…) でチャット履歴スクロール
pandas>=2.0.0           # チャット UI の表・グラフ用

# --- Experiment Tracking (Optional) ---
# tensorboard           # 学習曲線のモニタリング
